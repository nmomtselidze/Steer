[project]
name = "steer3dprj"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nodar", email = "nmomtselidze@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.9",
    "torch>=2.14.0",
]

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"